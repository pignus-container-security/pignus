# Get All Images

Gets all images in the Pignus database.

**URL** : `/api/images/`

**Method** : `GET`

**Auth required** : YES

**Permissions required** : `get-all-images`

**Data** : `{}`

## Success Response

**Code** : `200 OK`

**Content** : 
    ```
    {
        "data"
    }
    ```
