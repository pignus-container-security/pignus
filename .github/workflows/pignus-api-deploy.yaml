# # Helm Release
# # This workflow releases the Pignus-Api to staging environment

# name: Pignus-Api Deploy

# on:
#   pull_request:
#     branches: "staging"

# jobs:
#   deploy:
#     runs-on: self-hosted

#     steps:

#     - uses: actions/checkout@v3
#     - name: Set up Python ${{ matrix.python-version }}
#       uses: actions/setup-python@v3
#       with:
#         python-version: ${{ matrix.python-version }}

