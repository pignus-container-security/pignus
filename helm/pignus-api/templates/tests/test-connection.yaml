apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "pignus-api.fullname" . }}-test-connection"
  labels:
    {{- include "pignus-api.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "pignus-api.fullname" . }}:{{ .Values.pignus_api.service.port }}']
  restartPolicy: Never
