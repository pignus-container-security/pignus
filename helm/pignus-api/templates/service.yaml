apiVersion: v1
kind: Service
metadata:
  name: {{ include "pignus-api.fullname" . }}
  labels:
    {{- include "pignus-api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.pignus_api.service.type }}
  ports:
    - port: {{ .Values.pignus_api.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "pignus-api.selectorLabels" . | nindent 4 }}
