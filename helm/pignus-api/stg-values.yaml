# Default values for theia.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

replicaCount: 1

pignus_api:
  name: 'pignus-api'
  database:
    host: "mysql-stg.pignus-stg.svc.cluster.local"
    name: "pignus_stg"

  image:
    tag: "0.0.1"
    pullPolicy: IfNotPresent

  service:
    type: NodePort
    port: 8080


pignus_sentry:
  name: "pignus-sentry"
  debug: true
  api_host: "http://192.168.50.50:30866"

  image:
    tag: "0.0.1"
    pullPolicy: IfNotPresent
